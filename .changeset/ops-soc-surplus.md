---
"ftw-webapp": patch
---

Two new command operations in the contract: `loadpoint.soc.set` corrects the car's current charge level and `loadpoint.surplus_only.set` turns PV-only charging on or off. Contract only; the controls come in a later release.
