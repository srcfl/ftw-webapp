---
"ftw-webapp": patch
---

Make the installed app feel immediate: keep tab changes, live status, warm starts, updates and pull-to-refresh in place without false live states or full-page reloads.

Guide first-time users through Home Screen install, the pairing QR in the box's local dashboard and passkey recovery. Add a separate in-memory demo of a running home that never reads or writes real home data.
