---
"ftw-webapp": patch
---

Let the FTW relay accept the box's anonymous daily fleet report and keep only 90 days of UTC-day totals. The local operator route shows reports by version, channel, driver type, battery range, price zone and install-age range; it never stores source addresses or raw reports and does not claim to count unique boxes.
