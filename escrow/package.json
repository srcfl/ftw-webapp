{
  "name": "@srcfl/ftw-escrow",
  "version": "0.0.0",
  "private": true,
  "description": "The sealed escrow: it holds ciphertext under an opaque id and nothing beside it.",
  "type": "module",
  "engines": { "node": ">=26" },
  "scripts": {
    "start": "node src/main.ts"
  }
}
